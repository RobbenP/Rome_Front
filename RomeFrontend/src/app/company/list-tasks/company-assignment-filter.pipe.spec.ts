import { CompanyAssignmentFilterPipe } from './company-assignment-filter.pipe';

describe('CompanyAssignmentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyAssignmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
