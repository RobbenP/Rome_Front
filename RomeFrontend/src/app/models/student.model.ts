export class Student {
    constructor(
        public studentID : number, public nickName: string, public name: string, public birthDay: Date, public biography: string, public linkedIn: string, public experience: string, public phoneNumber: string 
    ){}
}
