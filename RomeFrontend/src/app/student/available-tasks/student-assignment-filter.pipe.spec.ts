import { StudentAssignmentFilterPipe } from './student-assignment-filter.pipe';

describe('StudentAssignmentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentAssignmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
